pandas>=2.0,<3
numpy>=1.24,<3
pyarrow>=14,<25
# Note: torch is required to run train.py and reproduce the model.
# Install CPU-only via: pip install torch --index-url https://download.pytorch.org/whl/cpu
